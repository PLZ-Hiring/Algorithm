const fs = require("fs");
const input = fs.readFileSync(0, "utf8").trim().split("\n");

const price = input[0].split(" ").map(Number); // [A, B, C]

const time = new Array(101).fill(0);
let max = -Infinity;

for (let i = 1; i <= 3; i++) {
  const [from, to] = input[i].split(" ").map(Number);
  max = Math.max(max, from, to);

  for (let t = from; t < to; t++) {
    time[t]++;
  }
}

let result = 0;

for (let t = 1; t <= max; t++) {
  if (time[t] === 1) result += price[0];       // 1대 주차
  else if (time[t] === 2) result += price[1] * 2; // 2대
  else if (time[t] === 3) result += price[2] * 3; // 3대
}

console.log(result);
/*✅ 1. 문제 요약

이 문제는 트럭 3대가 각각 언제 들어오고 언제 나갔는지가 주어졌을 때,
같은 시간에 1대, 2대, 3대 주차되어 있었다면
각각 A, B, C 요금을 적용하여 총 주차 요금을 구하는 문제입니다.

이 문제는 한 시간대에 몇 대가 있었는지가 핵심이기 때문에,

시간을 1~100까지 칸으로 생각하고

각 시간대에 차가 있으면 +1씩 증가시키고

나중에 시간별로 차량 수에 따라 요금을 더하는 방식으로 해결했습니다.

time[t]에는 “시간 t에 동시에 있는 트럭 수”가 저장됩니다.

시간은 1~100이므로 크기 101의 배열을 0으로 초기화합니다.

max는 가장 늦게까지 주차된 시각을 기록하기 위한 변수입니다

3대 트럭의 (from, to)를 읽어옵니다.

t < to 이기 때문에 퇴장 시간은 포함되지 않습니다.

예를 들어 2 5라면 2, 3, 4 시간 동안만 주차한 것과 같습니다.

해당 시간마다 time[t]를 1씩 증가시켜
→ 결국 time[t] = 그 시간에 있는 차량의 수가 됩니다.

그리고 아래 반복문으로 한대두대세대 계산 */








/* 상근이는 트럭을 총 세 대 가지고 있다. 오늘은 트럭을 주차하는데 비용이 얼마나 필요한지 알아보려고 한다.

상근이가 이용하는 주차장은 주차하는 트럭의 수에 따라서 주차 요금을 할인해 준다.

트럭을 한 대 주차할 때는 1분에 한 대당 A원을 내야 한다. 두 대를 주차할 때는 1분에 한 대당 B원, 세 대를 주차할 때는 1분에 한 대당 C원을 내야 한다.

A, B, C가 주어지고, 상근이의 트럭이 주차장에 주차된 시간이 주어졌을 때, 주차 요금으로 얼마를 내야 하는지 구하는 프로그램을 작성하시오.

입력
첫째 줄에 문제에서 설명한 주차 요금 A, B, C가 주어진다. (1 ≤ C ≤ B ≤ A ≤ 100)

다음 세 개 줄에는 두 정수가 주어진다. 이 정수는 상근이가 가지고 있는 트럭이 주차장에 도착한 시간과 주차장에서 떠난 시간이다. 도착한 시간은 항상 떠난 시간보다 앞선다. 입력으로 주어지는 시간은 1과 100사이 이다./